# EKS Auto Mode Microservices Demo

This guide shows how to create an **Amazon EKS Auto Mode** cluster and deploy a simple website with multiple microservices.

The demo contains:

- A public **frontend** service exposed through an AWS Load Balancer.
- An internal **users-service** backend.
- An internal **orders-service** backend.
- Internal service-to-service communication using Kubernetes DNS.

---

## Architecture

```text
Internet
   ↓
AWS Network Load Balancer
   ↓
frontend-service  ← public LoadBalancer
   ↓
frontend pod: nginx
   ├── /api/users  → users-service   ← internal ClusterIP
   └── /api/orders → orders-service  ← internal ClusterIP
                         ↓
                    calls users-service internally
```

---

## 0. Prerequisites

Install these locally:

```bash
aws --version
kubectl version --client
eksctl version
```

Configure AWS:

```bash
aws configure
```

Use Mumbai region:

```bash
export AWS_REGION=ap-south-1
export CLUSTER_NAME=demo-eks-auto
```

Check identity:

```bash
aws sts get-caller-identity
```

---

## 1. Create EKS Auto Mode Cluster

Simplest command:

```bash
eksctl create cluster \
  --name $CLUSTER_NAME \
  --region $AWS_REGION \
  --enable-auto-mode
```

This creates an **EKS Auto Mode** cluster.

Update kubeconfig:

```bash
aws eks update-kubeconfig \
  --name $CLUSTER_NAME \
  --region $AWS_REGION
```

Verify:

```bash
kubectl get nodes
kubectl get ns
```

---

## 2. Create Project Folder

```bash
mkdir eks-microservices-demo
cd eks-microservices-demo
```

---

## 3. Create Namespace

Create the namespace file:

```bash
cat > 00-namespace.yaml <<'EOF'
apiVersion: v1
kind: Namespace
metadata:
  name: micro-demo
EOF
```

Apply it:

```bash
kubectl apply -f 00-namespace.yaml
```

---

## 4. Users Microservice

This is an internal backend service. It exposes:

```text
/users
/health
```

Create the YAML:

```bash
cat > 01-users-service.yaml <<'EOF'
apiVersion: v1
kind: ConfigMap
metadata:
  name: users-app-code
  namespace: micro-demo
data:
  app.py: |
    from flask import Flask, jsonify

    app = Flask(__name__)

    @app.route("/")
    def home():
        return jsonify({
            "service": "users-service",
            "message": "Users service is running"
        })

    @app.route("/users")
    def users():
        return jsonify({
            "service": "users-service",
            "users": [
                {"id": 1, "name": "Tushar"},
                {"id": 2, "name": "Aman"},
                {"id": 3, "name": "Ravi"}
            ]
        })

    @app.route("/health")
    def health():
        return jsonify({"status": "ok"})

    if __name__ == "__main__":
        app.run(host="0.0.0.0", port=5000)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-deployment
  namespace: micro-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: python:3.12-slim
          command: ["/bin/sh", "-c"]
          args:
            - pip install flask && python /app/app.py
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: users-code
              mountPath: /app
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
      volumes:
        - name: users-code
          configMap:
            name: users-app-code
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: micro-demo
spec:
  type: ClusterIP
  selector:
    app: users
  ports:
    - port: 5000
      targetPort: 5000
EOF
```

Apply it:

```bash
kubectl apply -f 01-users-service.yaml
```

---

## 5. Orders Microservice

This service exposes:

```text
/orders
/health
```

It also calls `users-service` internally using Kubernetes DNS:

```text
http://users-service:5000/users
```

That works because both services are inside the same namespace.

Create the YAML:

```bash
cat > 02-orders-service.yaml <<'EOF'
apiVersion: v1
kind: ConfigMap
metadata:
  name: orders-app-code
  namespace: micro-demo
data:
  app.py: |
    from flask import Flask, jsonify
    import requests

    app = Flask(__name__)

    USERS_SERVICE_URL = "http://users-service:5000/users"

    @app.route("/")
    def home():
        return jsonify({
            "service": "orders-service",
            "message": "Orders service is running"
        })

    @app.route("/orders")
    def orders():
        try:
            users_response = requests.get(USERS_SERVICE_URL, timeout=2)
            users_data = users_response.json()
        except Exception as e:
            users_data = {
                "error": "Could not call users-service",
                "details": str(e)
            }

        return jsonify({
            "service": "orders-service",
            "orders": [
                {"order_id": 101, "item": "Helmet", "user_id": 1},
                {"order_id": 102, "item": "Riding Gloves", "user_id": 2},
                {"order_id": 103, "item": "Bike Jacket", "user_id": 3}
            ],
            "internal_call_to_users_service": users_data
        })

    @app.route("/health")
    def health():
        return jsonify({"status": "ok"})

    if __name__ == "__main__":
        app.run(host="0.0.0.0", port=5000)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders-deployment
  namespace: micro-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: orders
  template:
    metadata:
      labels:
        app: orders
    spec:
      containers:
        - name: orders
          image: python:3.12-slim
          command: ["/bin/sh", "-c"]
          args:
            - pip install flask requests && python /app/app.py
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: orders-code
              mountPath: /app
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
      volumes:
        - name: orders-code
          configMap:
            name: orders-app-code
---
apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: micro-demo
spec:
  type: ClusterIP
  selector:
    app: orders
  ports:
    - port: 5000
      targetPort: 5000
EOF
```

Apply it:

```bash
kubectl apply -f 02-orders-service.yaml
```

---

## 6. Frontend Website

The frontend is an nginx website.

It serves HTML and proxies API calls:

```text
/api/users  → users-service:5000/users
/api/orders → orders-service:5000/orders
```

Create the YAML:

```bash
cat > 03-frontend.yaml <<'EOF'
apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-html
  namespace: micro-demo
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>EKS Auto Mode Microservices Demo</title>
      <style>
        body {
          font-family: Arial, sans-serif;
          background: #f5f7fb;
          margin: 0;
          padding: 40px;
        }
        .container {
          max-width: 900px;
          margin: auto;
          background: white;
          padding: 30px;
          border-radius: 16px;
          box-shadow: 0 8px 24px rgba(0,0,0,0.08);
        }
        h1 {
          color: #111827;
        }
        button {
          padding: 12px 20px;
          margin: 8px;
          border: none;
          border-radius: 8px;
          background: #2563eb;
          color: white;
          cursor: pointer;
          font-size: 15px;
        }
        button:hover {
          background: #1d4ed8;
        }
        pre {
          background: #111827;
          color: #22c55e;
          padding: 20px;
          border-radius: 12px;
          overflow-x: auto;
        }
        .box {
          margin-top: 20px;
          padding: 16px;
          background: #eef2ff;
          border-radius: 12px;
        }
      </style>
    </head>
    <body>
      <div class="container">
        <h1>EKS Auto Mode Microservices Demo</h1>
        <p>This website is running on Kubernetes EKS Auto Mode.</p>

        <div class="box">
          <p><b>Architecture:</b></p>
          <p>Browser → AWS Load Balancer → Frontend nginx → Internal Kubernetes Services → Backend Pods</p>
        </div>

        <button onclick="callUsers()">Call Users Service</button>
        <button onclick="callOrders()">Call Orders Service</button>

        <h2>Response</h2>
        <pre id="output">Click a button to call backend microservices...</pre>
      </div>

      <script>
        async function callUsers() {
          const res = await fetch('/api/users');
          const data = await res.json();
          document.getElementById('output').textContent = JSON.stringify(data, null, 2);
        }

        async function callOrders() {
          const res = await fetch('/api/orders');
          const data = await res.json();
          document.getElementById('output').textContent = JSON.stringify(data, null, 2);
        }
      </script>
    </body>
    </html>
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-nginx-config
  namespace: micro-demo
data:
  default.conf: |
    server {
      listen 80;

      location / {
        root /usr/share/nginx/html;
        index index.html;
      }

      location /api/users {
        proxy_pass http://users-service:5000/users;
      }

      location /api/orders {
        proxy_pass http://orders-service:5000/orders;
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: micro-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: frontend-html-volume
              mountPath: /usr/share/nginx/html
            - name: frontend-nginx-config-volume
              mountPath: /etc/nginx/conf.d
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
      volumes:
        - name: frontend-html-volume
          configMap:
            name: frontend-html
        - name: frontend-nginx-config-volume
          configMap:
            name: frontend-nginx-config
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: micro-demo
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: internet-facing
spec:
  type: LoadBalancer
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
EOF
```

Apply it:

```bash
kubectl apply -f 03-frontend.yaml
```

Because this service is `type: LoadBalancer`, EKS Auto Mode provisions a Network Load Balancer by default.

---

## 7. Check Everything

```bash
kubectl get pods -n micro-demo
kubectl get svc -n micro-demo
kubectl get deployments -n micro-demo
```

Wait until all pods are running:

```bash
kubectl wait --for=condition=available deployment/users-deployment -n micro-demo --timeout=180s
kubectl wait --for=condition=available deployment/orders-deployment -n micro-demo --timeout=180s
kubectl wait --for=condition=available deployment/frontend-deployment -n micro-demo --timeout=180s
```

Get the Load Balancer URL:

```bash
kubectl get svc frontend-service -n micro-demo
```

You will see something like:

```text
NAME               TYPE           CLUSTER-IP      EXTERNAL-IP                                                                    PORT(S)
frontend-service   LoadBalancer   10.x.x.x        abc123.elb.ap-south-1.amazonaws.com                                            80:xxxxx/TCP
```

Open in browser:

```text
http://<EXTERNAL-IP>
```

Example:

```text
http://abc123.elb.ap-south-1.amazonaws.com
```

---

## 8. Test Backend APIs

Replace the URL with your actual Load Balancer DNS:

```bash
export LB_URL=$(kubectl get svc frontend-service -n micro-demo -o jsonpath='{.status.loadBalancer.ingress[0].hostname}')

echo $LB_URL
```

Test frontend:

```bash
curl http://$LB_URL
```

Test users API:

```bash
curl http://$LB_URL/api/users
```

Test orders API:

```bash
curl http://$LB_URL/api/orders
```

In `/api/orders`, you should see that `orders-service` internally called `users-service`.

---

## 9. Understand How Microservices Communicate

Inside Kubernetes, every Service gets an internal DNS name.

Since all services are in the same namespace, these work:

```text
http://users-service:5000/users
http://orders-service:5000/orders
```

Full DNS names:

```text
http://users-service.micro-demo.svc.cluster.local:5000/users
http://orders-service.micro-demo.svc.cluster.local:5000/orders
```

Frontend to users service flow:

```text
Browser
  ↓
frontend-service LoadBalancer
  ↓
frontend pod nginx
  ↓
users-service ClusterIP
  ↓
users pods
```

Frontend to orders service flow:

```text
Browser
  ↓
frontend-service LoadBalancer
  ↓
frontend pod nginx
  ↓
orders-service ClusterIP
  ↓
orders pods
  ↓
users-service ClusterIP
  ↓
users pods
```

---

## 10. Scale Microservices

Scale users service:

```bash
kubectl scale deployment users-deployment -n micro-demo --replicas=4
```

Scale orders service:

```bash
kubectl scale deployment orders-deployment -n micro-demo --replicas=3
```

Check pods:

```bash
kubectl get pods -n micro-demo -o wide
```

Kubernetes Service automatically load balances traffic across matching pods.

---

## 11. See Logs

Frontend logs:

```bash
kubectl logs -n micro-demo deployment/frontend-deployment
```

Users logs:

```bash
kubectl logs -n micro-demo deployment/users-deployment
```

Orders logs:

```bash
kubectl logs -n micro-demo deployment/orders-deployment
```

---

## 12. Debug Inside Cluster

Run a temporary curl pod:

```bash
kubectl run curl-test \
  -n micro-demo \
  --image=curlimages/curl \
  --rm -it \
  -- sh
```

Inside the pod:

```bash
curl http://users-service:5000/users
curl http://orders-service:5000/orders
exit
```

This proves internal service-to-service communication.

---

## 13. Recommended Infrastructure for a Real App

For learning, the above setup is enough.

For a real microservices app on EKS Auto Mode, use this:

| Layer | Recommended Setup |
|---|---|
| Cluster | EKS Auto Mode |
| Public entry | ALB Ingress or NLB Service |
| Frontend | Deployment + Service |
| Backend microservices | Deployment + internal ClusterIP Services |
| Config | ConfigMap |
| Secrets | AWS Secrets Manager + External Secrets Operator / ESO |
| Database | RDS PostgreSQL / Aurora |
| Cache | ElastiCache Redis |
| Container registry | ECR |
| CI/CD | GitHub Actions / Argo CD |
| Observability | CloudWatch Container Insights, Prometheus, Grafana |
| TLS | ACM certificate + ALB Ingress |
| DNS | Route 53 |
| Autoscaling | HPA + EKS Auto Mode managed compute |

For this demo, we intentionally used `Service type LoadBalancer` because it is simple and Auto Mode handles NLB provisioning directly.

For production path-based routing like `/users`, `/orders`, `/payments`, **ALB Ingress** is usually better.

---

## 14. Delete Everything to Avoid AWS Cost

First delete the app:

```bash
kubectl delete namespace micro-demo
```

Then delete the cluster:

```bash
eksctl delete cluster \
  --name $CLUSTER_NAME \
  --region $AWS_REGION
```

Verify the Load Balancer is gone from the AWS Console after deletion.

---

## One-Shot Copy-Paste Summary

```bash
export AWS_REGION=ap-south-1
export CLUSTER_NAME=demo-eks-auto

eksctl create cluster \
  --name $CLUSTER_NAME \
  --region $AWS_REGION \
  --enable-auto-mode

aws eks update-kubeconfig \
  --name $CLUSTER_NAME \
  --region $AWS_REGION

kubectl get nodes
```

Then apply the four YAML files in order:

```bash
kubectl apply -f 00-namespace.yaml
kubectl apply -f 01-users-service.yaml
kubectl apply -f 02-orders-service.yaml
kubectl apply -f 03-frontend.yaml
```

Get the URL:

```bash
kubectl get svc frontend-service -n micro-demo
```

Open:

```text
http://<LOAD-BALANCER-DNS>
```

---

## Cleanup Summary

```bash
kubectl delete namespace micro-demo

eksctl delete cluster \
  --name $CLUSTER_NAME \
  --region $AWS_REGION
```
